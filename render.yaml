services:
  - type: web
    name: gridlock-forecasting
    env: python
    rootDir: forecasting
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
